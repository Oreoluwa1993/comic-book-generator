# Copy Learnings — Cintelytics / Terminal

This file is maintained automatically by the `/copy-improver` skill.
Each run appends new findings. Never edit the standing rules by hand — let the
skill update them as more data comes in.

## Standing rules (none yet — populate after first run)

_No runs yet. Run `/copy-improver cintelytics-terminal` after your first campaign sends._
