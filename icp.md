# ICP files have moved

Per-client ICP documents now live in the client folder:

```
clients/{client-slug}/icp.md
```

**Current clients:**
- `clients/cintelytics-terminal/icp.md` — Cintelytics / Terminal

See `clients/_index.md` for the full client registry.
